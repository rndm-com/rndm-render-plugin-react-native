import View from './View';

const Picker = {
  ...View,
  onValueChange: null,
  selectedValue: null,
  testID: null,
  enabled: null,
  mode: null,
  prompt: null,
  itemStyle: null,
};

export default Picker;
